# dependencies
node_modules

# nuxt
.nuxt
.output
.data
dist

# database
*.db
*.db-journal
prisma/dev.db
prisma/dev.db-journal

# Local production DB dumps (required after each Liara deploy; never commit)
backups/

# env
.env
.env.*
!.env.example

# logs
*.log
.inbox-dev/

# os / editor
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json

.cache
.liara-secrets.env

# Local planning artifacts (doodles, HTML prototypes, PDFs)
planning/

# Local Canva exports (large images; do not commit)
canva-reference/pages/
canva-reference/contact-sheets/
canva-reference/comparisons/
canva-reference/*.zip
!canva-reference/README.md

# Shell / editor temp junk
sh-thd-*

# Watermarked Canva dumps (keep local; do not ship binaries)
canva-reference/inbox-website-2026-08-12/raw/
canva-reference/inbox-website-2026-08-12/diff-review/
canva-reference/inbox-website-2026-08-12/*.zip
